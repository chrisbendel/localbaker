<% content_for :title, "Profile" %>

<div class="stack">
  <h1>Profile</h1>

  <% unless current_user.store %>
    <div>
      <small class="text-muted">Want to sell your own baked goods? <%= link_to "Create your store", new_store_path %></small> 
    </div>
  <% end %>

  <%= form_with model: current_user, url: profile_path, method: :patch, class: "stack" do |f| %>
    <div>
      <%= f.label :email, "Email" %>
      <%= f.email_field :email, disabled: true %>
    </div>

    <div>
      <%= f.label :name, "Name" %>
      <%= f.text_field :name, placeholder: "Your name" %>
    </div>

    <%= f.submit "Save", class: "button primary", style: "align-self: flex-start" %>
  <% end %>

  <hr class="divider"/>
  <%= button_to "Sign out", session_path, method: :delete, class: "button text-muted", form: { style: "display: inline" } %>
</div>
