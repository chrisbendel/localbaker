Order confirmed: <%= @event.name %>

Hi <%= @order.user.email %>,

Your order for <%= @store.name %> has been confirmed!

Pickup Details:
When: <%= @event.pickup_starts_at.strftime("%A, %b %-d") %>, <%= pickup_time(@event.pickup_starts_at) %>–<%= pickup_time(@event.pickup_ends_at) %>
Where: <%= @event.address %>

Your Order Summary:
<% @order.order_items.includes(:event_product).each do |item| %>
- <%= item.quantity %> × <%= item.event_product.name %>
<% end %>

Total: <%= number_to_currency(@order.total_price) %>

View your order here: <%= orders_url %>

Thanks for supporting a local baker!
— The LocalBaker Team
