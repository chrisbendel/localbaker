<% content_for :title, "Edit #{@event.name}" %>

<h3>Edit Event</h3>

<%# Delete / cancel lives on the event show page (with the customer-notify
    flow), so there's no Danger Zone here — one place to destroy an event. %>
<%= render "form", event: @event, url: event_path(@event), submit_label: "Save Changes" %>
