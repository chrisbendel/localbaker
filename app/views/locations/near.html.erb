<% content_for :title, "Bakeries Near You" %>

<div class="container" data-controller="geolocation">
  <div class="stack-lg">
    <section class="stack-sm">
      <h1>Bakeries Near You</h1>
      <p>Find fresh baked goods and upcoming bakes in your area.</p>
    </section>

    <div class="panel stack">
      <%= form_with url: near_path, method: :get, class: "stack", data: { geolocation_target: "form" } do |f| %>
        <div class="stack-sm">
          <%= f.label :address, "Find bakeries near..." %>
          <%= f.text_field :address,
              value: @address,
              placeholder: "City, state or zip code",
              data: { geolocation_target: "addressInput" } %>
        </div>

        <div class="group">
          <button type="button"
                  class="button secondary"
                  data-action="geolocation#requestLocation"
                  data-geolocation-target="locationButton">
            Use My Location
          </button>
          <%= f.submit "Search", class: "button primary" %>
        </div>

        <%= f.hidden_field :latitude, value: @latitude, data: { geolocation_target: "latitude" } %>
        <%= f.hidden_field :longitude, value: @longitude, data: { geolocation_target: "longitude" } %>
      <% end %>

      <% if @latitude.present? && @longitude.present? %>
        <small>Showing results within <%= @radius %> miles</small>
      <% end %>
    </div>

    <% if @stores.any? %>
      <div class="stack">
        <% @stores.each do |store| %>
          <%= render "locations/bakery_card", store: store, distance: store.try(:distance), upcoming_event: @next_events_by_store[store.id] %>
        <% end %>
      </div>
    <% elsif @latitude.present? %>
      <div class="empty-state stack-sm">
        <p>No bakeries found within <%= @radius %> miles.</p>
        <p>Try expanding your search radius or a different location.</p>
      </div>
    <% else %>
      <div class="empty-state">
        <p>No bakeries are taking orders right now. Check back soon!</p>
      </div>
    <% end %>
  </div>
</div>
