# LocalBaker — Claude Instructions

**Read `AGENTS.md`** for stack, constraints, commands, and pointers to localized context files.

## Non-negotiables

- **You never stage, commit, push, branch, or open PRs.** You write code; Chris handles all git operations.
- **You never run tests.** Ask Chris to run `bin/validate` and share results; iterate on actual failures, not hypothetical ones.
- Use the `gh` CLI for GitHub operations (issues, PRs) — no URL guessing.
